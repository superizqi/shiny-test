services:
  - type: web
    name: shiny-test
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: shiny run app.py --port $PORT --host 0.0.0.0
    autoDeploy: true
